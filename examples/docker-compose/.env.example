# ATLAS Docker Compose Environment Variables
# 复制到 .env: cp .env.example .env

# Image configuration / 镜像配置
ATLAS_IMAGE=atlas:v0.6-base

# JupyterLab configuration / JupyterLab 配置
JUPYTER_PORT=8888
JUPYTER_TOKEN=atlas

# GPU configuration / GPU 配置
# Options: all, 0, 1, "0,1", etc.
GPU_DEVICES=all

# Host paths / 主机路径
HOST_WORKSPACE=./workspace
